Assemble for Linux with:
```bash
gcc -o ./flappybird -s flappybird.s -no-pie
```
And run with:
```bash
./flappybird
```
It is important to run the executable in the same folder as the `highscore.txt` file.